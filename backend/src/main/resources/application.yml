server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://postgres.postgres.svc.cluster.local:5432/inventory_db
    username: inventory_user
    password: inventory_pass

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
